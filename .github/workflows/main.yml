name: Build Rave Ghost
on: [push, pull_request]

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Theos
        run: |
          bash -c "$(curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos)"
          echo "THEOS=~/theos" >> $GITHUB_ENV

      - name: Build Tweak
        run: |
          export THEOS=~/theos
          # تحميل SDK مضغوط وسريع
          curl -L https://github.com/theos/sdks/raw/master/iPhoneOS14.5.sdk.tar.xz -o sdk.tar.xz
          mkdir -p $THEOS/sdks
          tar -xf sdk.tar.xz -C $THEOS/sdks
          
          # البدء بالبناء مع تجاهل التحذيرات
          make package FINALPACKAGE=1 ALWAYS_CLEAN=1
          
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: RaveGhost_Final_Success
          path: packages/*.deb
